---
// Root → default-locale homepage redirect.
// Geo-detection can be layered in later via Cloudflare headers (cf-ipcountry).
import { defaultLocale } from "~/lib/config";
return Astro.redirect(`/${defaultLocale}/`, 302);
---
